name: 自動測試與部署

# 什麼時候執行這個 Action
on:
  push:
    branches: [main] # 當推送到 main 分支時
  pull_request:
    branches: [main]

# 要執行的工作
jobs:
  test-and-deploy:
    runs-on: ubuntu-latest # 在 Ubuntu 環境執行

    steps:
      # 1. 下載程式碼
      - name: 📥 下載程式碼
        uses: actions/checkout@v3

      # 2. 檢查 HTML 語法
      - name: 🔍 檢查 HTML 語法
        uses: Cyb3r-Jak3/html5-validator-action@v7.2.0
        with:
          root: ./
          css: true

      # 3. 部署到 GitHub Pages
      - name: 🚀 部署到 GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        if: github.ref == 'refs/heads/main'
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./
